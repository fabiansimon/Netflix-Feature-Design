export type Movie = {
    releaseDate: number;
    length: number;
    description: string;
    thumbnailUri: string;
    minAge: number;
    cast: string[];
    genres: string[];
    tags: string[];
}